# Gate 10 Report

Evidence in this directory was generated by
`tools/security/run-security-checks.sh` invoked against the current
branch tip.

| Check | Tool | Result | Evidence file |
|---|---|---|---|
| Toolchain versions | in-house verifier | PASS — 5/5 tools present at pinned version | `TOOLCHAIN_VERSIONS.json` |
| Dependency audit | cargo-audit 0.22.2 | PASS — 0 vulnerabilities | `DEPENDENCY_AUDIT.json` |
| Disposition cross-reference | check-dispositions.py | PASS — 0 unresolved | `DEPENDENCY_AUDIT_DISPOSITIONED.json` |
| Secret scan | gitleaks 8.28.0 | PASS — 0 findings across full history | `SECRET_SCAN.json` |
| Static analysis | rustfmt + clippy (deny warnings) | PASS — 0 clippy warnings | `STATIC_ANALYSIS.json` |
| Unsafe-code inventory | grep | PASS — every crate is `#![forbid(unsafe_code)]` | `UNSAFE_CODE_INVENTORY.txt` |
| SBOM generation | cargo-cyclonedx 0.5.7 | PASS — 10 SBOMs, all valid | `SBOM_INDEX.json`, `sbom/*.cdx.json` |
| Policy summary | in-house verifier | PASS | `SECURITY_POLICY_RESULTS.json` |

## Gate 10 decision

**PASS** — pinned security tools installed in a clean environment, every
required check executed and produced machine-readable evidence, policy
was enforced (no silent skips, no advisory-only downgrades), and no
finding was masked.

## Reproduction

```
tools/security/install-security-tools.sh       # or --check
tools/security/run-security-checks.sh          # exits non-zero on any failure
```

## Notes

- `cargo-audit` was bumped from the initial pin (0.21.1) to 0.22.2 in
  this branch after the RustSec advisory database began including
  CVSS 4.0 vectors that older cargo-audit versions cannot parse.
- The vulnerability-disposition ledger
  (`cybersecurity/vulnerability-dispositions.json`) is empty at the
  time of this report because `cargo-audit --json` returned zero
  findings against the current `Cargo.lock`. Future findings must be
  dispositioned under
  `cybersecurity/VULNERABILITY_DISPOSITION_POLICY.md`.
