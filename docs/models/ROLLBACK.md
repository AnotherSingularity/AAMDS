# Model Rollback

See [`../operations/ROLLBACK.md`](../operations/ROLLBACK.md) — model
rollback section.

Rollback is `Deprecated → Approved → Active`. It re-uses the existing
artifact digest and signature; no new deployment is required. A
`REVOKED` model cannot be rolled back into service.
